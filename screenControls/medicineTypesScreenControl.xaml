<UserControl x:Class="pharmacyManagement.screenControls.medicineTypesScreenControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:pharmacyManagement.screenControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="control_Loaded">


    <UserControl.Resources >
        <DataTemplate x:Key="dtTemplate1">
            <Grid>
                <Grid Background="Azure"  >
                    <ListBox >
                        <ListBoxItem >
                            <Border  Background="{StaticResource listItemBackgroundColor }"
                                    BorderBrush="{StaticResource PrimaryBlueColor }"
                     BorderThickness="2"
                     Height=" 30"
                     Width="700"
                     CornerRadius="12">
                                <Grid >
                                    <Grid.ColumnDefinitions >
                                        <ColumnDefinition Width="4*"
                                                          />
                                        <ColumnDefinition Width="50" />
                                        <ColumnDefinition Width="40"/>
                                    </Grid.ColumnDefinitions>
                                    <Label Grid.Column ="0"
                         Content="{Binding Path=type}"
                                FontSize="14"
                                FontWeight="Bold"
                                Margin="20,0,0,0"/>
                                    <Button  Grid.Column="1"
                                                 Cursor="Hand"
                                                 Click="btnEdit_click">
                                        <Button.Template >
                                            <ControlTemplate >
                                                <Image 
                                                    Source="/images/editBtnImg.png" 
                                                    Width="25"
                                                    VerticalAlignment="Center" />
                                                </ControlTemplate> 
                                        </Button.Template>
                                    </Button>
                                    <Button  Grid.Column="2"
                                     Cursor="Hand"
                                     Click="btnDelete_click">
                                        
                                        <Button.Template >
                                            <ControlTemplate >
                                                <Image Source="/images/deleteBtnImg.png"  
                                                       Width="25"
                                                      HorizontalAlignment="Left"   />
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                            </Border>
                            
                        </ListBoxItem>
                        
                    </ListBox>
                    
                </Grid>
                
            </Grid>
            
        </DataTemplate>
        
    </UserControl.Resources>
    <Grid >
        <ListBox  x:Name="lstMedicineTypeView"
                  ItemTemplate="{StaticResource dtTemplate1 }"
                  Background="Transparent" 
                  BorderBrush="Transparent ">
        </ListBox>
        <Button Content="mfjnfdj" 
         Width="50"
         Height="50"
         HorizontalAlignment="Right" 
         VerticalAlignment="Bottom" 
         Margin="0,0,30,30"
         Click="btnAdd_click"
         Cursor="Hand" >
            <Button.Template >
                <ControlTemplate >
                    <Image Source="/images/addBtnImg.png" 
                    Width="45"
                    />
                </ControlTemplate>
            </Button.Template>

        </Button>

    </Grid>
    
</UserControl>
